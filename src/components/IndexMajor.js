import React from "react";

class IndexMajor extends React.Component {
  render() {
    return (
      <header className="major">
        <h2>{this.props.text}</h2>
      </header>
    );
  }

}

export default IndexMajor;
